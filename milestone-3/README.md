Our code can be run using `python src/main.py`.  
It takes approximately 25 minutes to train the models and run GridSearch to find the bset hyperparamters,  
and then make predictions on the test data.
